/**
 * Generic, framework-free workspace and host-service contracts shared between the AskAI host and workspace
 * plugins. Java 8 + Swing/AWT only; no PF4J, no research-specific types, no askai-app implementation types.
 */
package com.aresstack.askai.plugin.api;
