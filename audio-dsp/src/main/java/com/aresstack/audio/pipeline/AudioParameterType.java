package com.aresstack.audio.pipeline;

/** The kinds of block parameter the inspector renders and the validator range-checks. */
public enum AudioParameterType {
    INTEGER,
    DECIMAL,
    BOOLEAN,
    CHOICE
}
