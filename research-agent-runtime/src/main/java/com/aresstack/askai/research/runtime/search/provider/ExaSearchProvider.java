package com.aresstack.askai.research.runtime.search.provider;

/**
 * Provider-specific contract for Exa ({@link SearchProviderId#EXA}). Catalogued only: a forward contract,
 * no concrete implementation in the first stage.
 */
public interface ExaSearchProvider extends SearchProvider {
}
