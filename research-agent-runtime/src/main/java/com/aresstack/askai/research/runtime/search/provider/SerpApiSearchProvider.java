package com.aresstack.askai.research.runtime.search.provider;

/**
 * Provider-specific contract for SerpApi ({@link SearchProviderId#SERP_API}). Catalogued only: the interface
 * exists as a forward contract; no concrete implementation is planned for the first stage.
 */
public interface SerpApiSearchProvider extends SearchProvider {
}
