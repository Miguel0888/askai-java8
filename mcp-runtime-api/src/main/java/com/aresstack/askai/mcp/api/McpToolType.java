package com.aresstack.askai.mcp.api;

/** The small, flat set of tool-parameter types the MVP exposes. Kept minimal so small models stay reliable. */
public enum McpToolType {
    STRING,
    INTEGER,
    BOOLEAN,
    ENUM
}
