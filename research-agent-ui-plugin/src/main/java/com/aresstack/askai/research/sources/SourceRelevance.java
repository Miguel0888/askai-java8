package com.aresstack.askai.research.sources;

/** How relevant a source is to the research question (validated, bounded value). */
public enum SourceRelevance {
    UNKNOWN,
    LOW,
    MEDIUM,
    HIGH
}
