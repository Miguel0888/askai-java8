package com.aresstack.askai.browser.search;

/** Requested reasoning effort for an AI call; DEFAULT leaves the model profile's own setting untouched. */
public enum ReasoningEffort {
    DEFAULT, LOW, MEDIUM, HIGH
}
